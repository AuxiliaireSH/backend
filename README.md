# README

First install ruby with your favorite package manager.
```shell
$ gem install bundler
$ git clone <this-repo>
$ cd <this-repo>
$ bundler install
$ rake db:migrate
$ rake db:seed
$ rails s
```
profit!

